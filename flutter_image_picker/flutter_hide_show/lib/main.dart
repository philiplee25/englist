import 'package:flutter/material.dart';

import 'camera_widget.dart';

void main() {
  runApp(
      MaterialApp(
        title: "Pick Image Camera",
        home: CameraWidget() ,
      )
     );
}

